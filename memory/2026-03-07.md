## 2026-03-07 16:04 Europe/London — VPS SSH hardening session

- User clarified target architecture: OpenClaw is **not** installed on the VPS; hardening performed via provider web terminal relay + direct SSH workflow.
- Confirmed user preferences/constraints:
  - Keep SSH reachable from anywhere temporarily.
  - Keep web ports open (HTTP/HTTPS) for service access.
  - Prioritize staged changes with lockout safety and provider console fallback.
- Actions completed on VPS (OS-level hardening path):
  - Created/verified non-root admin user with sudo privileges.
  - Set up key-based auth for that admin user.
  - Validated user could log in via SSH key from local machine before lock-down.
  - Disabled root SSH login.
  - Enforced password auth off for SSH; kept pubkey auth on.
  - Validated SSH config syntax and reloaded SSH service successfully.
- Security handling notes:
  - A temporary bootstrap key was generated server-side during setup, later removed from server.
  - User was instructed to rotate exposed credentials and rely on locally generated key material.
- Outcome:
  - SSH posture moved to key-only, non-root access model with successful continuity checks.
  - Final verification step requested from user: admin-key login succeeds; root SSH login fails.
- Next steps suggested:
  - Optional: restrict SSH source IP once stable.
  - Optional: add fail2ban/ufw tuning, unattended security updates, and periodic audit cadence.
